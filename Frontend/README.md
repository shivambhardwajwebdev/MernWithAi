## we are using npm i sass so that react recognize our style.scss file 
 
 ### now we using npm i react-router to use react router for making navigation bar

 ## to make already have an account link working we are importing  Link from react router
 and adding a line <p>Already have an account? <Link to ={"/login"}>Login</Link></p>
 import{Link} from 'react-router'
